@model IEnumerable<Models.EF.BRAND>
@{ Layout = null; }
@if (Model != null)
{
    foreach (var category in Model)
    {
        <li>
            <a href="@Url.Action("ShopCategory", "Shop", new { url = category.BrandURL })">
                @category.BrandName
            </a>
        </li>
    }
}